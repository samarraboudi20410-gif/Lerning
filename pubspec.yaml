name: elearningapplication
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.2 <4.0.0' # Mis à jour pour une version stable

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1 # L'indentation est maintenant correcte
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  uuid: ^3.0.7 



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/home.png
    - assets/flutterlearn_logo.png # L'indentation est maintenant correcte et alignée